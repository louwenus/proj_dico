recherche du verbe
recherche de promon {je tu il elle on nous vous ils elles}
si il n'est pas un pronom :
    - recherche dans le dictionnaire, 2 cas :
        1) est présent dans le dico mot au singulier => il / elle / on
        2) n'est pas dans le dictionnaire => pluriels d'un autre mot

ce qu'il faut faire
- creer un dico rempli de tous les verbes fct(fichier_dico) -> dico
- creer un dico rempli des promons fct() -> dico
- boucle qui explore le texte mot par mot et teste si le mot est un verbe
- fonction qui renvoie un index fct(pronom) -> index
- fonction qui conjuge le verbe (systéme d'index je:0, tu:1 il/elle/on:2 ...) fct(verbe à l'infinitif, index) -> verbe conjugés

ce qui a été fait :
- creer un dico rempli de tous les verbes fct(fichier_dico) -> dico
- fonction qui conjuge le verbe (systéme d'index je:0, tu:1 il/elle/on:2 ...) fct(verbe à l'infinitif, index) -> verbe conjugés

ce qui a été commencé :
- fonction qui renvoie un index fct(pronom) -> index